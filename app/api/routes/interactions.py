"""Legacy module.

Replaced by lead events:
- POST /api/leads/{lead_id}/events

This file is kept as a placeholder to avoid breaking imports during refactors.
"""

from fastapi import APIRouter

router = APIRouter()
